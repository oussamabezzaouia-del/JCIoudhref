{
  "name": "jci-oudhref",
  "private": true,
  "description": "Site JCI Oudhref — lance le serveur web (calendrier, galerie, API SQLite)",
  "scripts": {
    "start": "node server/server.js",
    "install-server": "npm install --prefix server"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "express": "^5.2.1",
    "multer": "^2.1.1",
    "nodejs": "^0.0.0"
  }
}
